The preprocess is run into the visual developer command prompt for visual studio 2017 
by firstly typing the disk in which the folder is contained and then 
calling that folder by 'cd' i.e change directory , after that the binarysearchtree.cpp and the test.cpp complied by 'cl/c binarysearchtree.cpp test.cpp' as the console displays 
compiled but for the library the 'makefile' is used in the makefile first command as "test"
called in prompt by nmake test.
 It will then display all the tests passed. 
The output file is also executed. 
Tools chain required is c compiler , assembler ,linker and the libraries which is contained in the visual studio 

For the single word count the command set it in makefile is "singlewords" 
called in prompt by nmake singlewords.
It will then display all the words and there count.


For the single word count the command set it in makefile is "sentences" 
called in prompt by nmake sentences.
It will then display all the words and there count.